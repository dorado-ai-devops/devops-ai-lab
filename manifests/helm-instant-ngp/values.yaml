image:
  repository: localhost:5000/nerf-trainer
  tag: "v0.1.3"
  pullPolicy: IfNotPresent

resources:
  limits:
    nvidia.com/gpu: 1
  requests:
    cpu: "500m"
    memory: "1Gi"
    nvidia.com/gpu: 1

volume:
  pvcName: pvc-datos-nerf
  mountPath: /data

datasetName: lego-ds

init:
  image:
    repository: localhost:5000/colmap-init
    tag: "v0.1.2"
    pullPolicy: IfNotPresent
